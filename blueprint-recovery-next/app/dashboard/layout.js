export const metadata = {
  robots: { index: false, follow: false },
  title: 'Coordinator Dashboard — Blueprint Recovery',
};

export default function DashboardLayout({ children }) {
  return children;
}
